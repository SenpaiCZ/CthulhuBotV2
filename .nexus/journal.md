## 2024-05-24 - Heal / Damage Context Menu -> **UX Fail:** GMs had to type complex commands or navigate menus to adjust player stats during combat. **UX Win:** Added a "Heal / Damage" Context Menu that opens a QuickStatAdjustModal to seamlessly adjust HP, SAN, and MP.
## 2024-05-24 - Context Menu Cleanup -> **UX Fail:** Assigning `description` or `binding` directly to `discord.app_commands.ContextMenu` resulted in `AttributeError` or `TypeError`, cluttering logic and logs. **UX Win:** Removed manual attribute assignments from ContextMenu instances across cogs and began relying on `getattr(cmd, 'binding', None)` when inspecting commands.
